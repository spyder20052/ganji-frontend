import type { Messages } from '../translate';

/** Traductions anglaises (profil, accueil après inscription, rendez-vous, notifications). Clé = phrase française exacte. */
export const compte: Messages = {};
