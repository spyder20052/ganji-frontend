import type { Messages } from '../translate';

/** Traductions en bariba (baatɔnum) (patient2). Clé = phrase française exacte. À valider par un locuteur natif. */
export const patient2: Messages = {};
