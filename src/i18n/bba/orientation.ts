import type { Messages } from '../translate';

/** Traductions en bariba (baatɔnum) (orientation). Clé = phrase française exacte. À valider par un locuteur natif. */
export const orientation: Messages = {};
