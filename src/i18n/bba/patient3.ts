import type { Messages } from '../translate';

/** Traductions en bariba (baatɔnum) (patient3). Clé = phrase française exacte. À valider par un locuteur natif. */
export const patient3: Messages = {};
