import type { Messages } from '../translate';

/** Traductions en bariba (baatɔnum) (commandes de médicaments et livraison). Clé = phrase française exacte. À valider par un locuteur natif. */
export const livraison: Messages = {};
