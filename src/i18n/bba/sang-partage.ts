import type { Messages } from '../translate';

/** Traductions en bariba (baatɔnum) (don de sang, demandes, documents partagés). Clé = phrase française exacte. À valider par un locuteur natif. */
export const sangPartage: Messages = {};
