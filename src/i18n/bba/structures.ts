import type { Messages } from '../translate';

/** Traductions en bariba (baatɔnum) (structures). Clé = phrase française exacte. À valider par un locuteur natif. */
export const structures: Messages = {};
