import type { Messages } from '../translate';

/** Traductions en bariba (baatɔnum) (public). Clé = phrase française exacte. À valider par un locuteur natif. */
export const publicPages: Messages = {};
