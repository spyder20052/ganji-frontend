import type { Messages } from '../translate';

/** Traductions en yoruba (landing-client). Clé = phrase française exacte. À valider par un locuteur natif. */
export const landingClient: Messages = {};
