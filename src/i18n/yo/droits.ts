import type { Messages } from '../translate';

/** Traductions en yoruba (droits, paiement, assistant de traitement). Clé = phrase française exacte. À valider par un locuteur natif. */
export const droits: Messages = {};
