import type { Messages } from '../translate';

/** Traductions en dendi (droits, paiement, assistant de traitement). Clé = phrase française exacte. À valider par un locuteur natif. */
export const droits: Messages = {};
