import type { Messages } from '../translate';

/** Traductions en dendi (don de sang, demandes, documents partagés). Clé = phrase française exacte. À valider par un locuteur natif. */
export const sangPartage: Messages = {};
