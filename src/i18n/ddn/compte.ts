import type { Messages } from '../translate';

/** Traductions en dendi (profil, accueil après inscription, rendez-vous, notifications). Clé = phrase française exacte. À valider par un locuteur natif. */
export const compte: Messages = {};
